import "core-js/modules/es.object.keys.js";
import i18next from 'i18next';
import { useTranslation as useTranslationOrig } from 'react-i18next';
import { i18nInitComponents } from './i18nInitComponents';
export var useTranslationBase = function useTranslationBase(en, ns, options) {
  var firstNameSpace = en.resources && Object.keys(en.resources.en || {})[0];
  var isEn = !i18next.language || i18next.language === 'en';
  var enResourcesLoaded = firstNameSpace && i18next.hasResourceBundle('en', firstNameSpace);

  if (isEn && !enResourcesLoaded) {
    i18nInitComponents(en);
  }

  return useTranslationOrig(ns, options);
};
//# sourceMappingURL=useTranslationBase.js.map